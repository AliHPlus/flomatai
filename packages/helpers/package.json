{
  "name": "@flomatai/helpers",
  "version": "0.1.2",
  "description": "Shared helpers for flomatai examples: LLM resolution from env vars, SQLite orchestrator factory",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@flomatai/core": "^0.1.1",
    "@flomatai/provider-anthropic": "^0.1.0",
    "@flomatai/provider-openai-compat": "^0.1.0",
    "@flomatai/provider-opencode-server": "^0.1.1",
    "@flomatai/state-sqlite": "^0.1.3"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
