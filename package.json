{
  "name": "flomatai",
  "version": "0.1.0",
  "private": true,
  "description": "A code-first AI automation framework built on Skills and Agents",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
